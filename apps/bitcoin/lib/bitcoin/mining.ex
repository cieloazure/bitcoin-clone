defmodule Bitcoin.Mining do
  def start_mining(latest_block, node) do
    # Hash of the latest block
    # Set difficulty target
    # Add nonce
    # If solution reached, start again 
    # send(node, {:blockchain_handler, :new_block, block})
  end
end
